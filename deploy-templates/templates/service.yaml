apiVersion: v1
kind: Service
metadata:
  name: {{ include "javascript-pnpm-angular.fullname" . }}
  labels:
    {{- include "javascript-pnpm-angular.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "javascript-pnpm-angular.selectorLabels" . | nindent 4 }}
