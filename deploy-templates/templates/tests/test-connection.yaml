apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "javascript-pnpm-angular.fullname" . }}-test-connection"
  labels:
    {{- include "javascript-pnpm-angular.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "javascript-pnpm-angular.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
